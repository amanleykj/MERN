Check the two videos that are on the calendar (how to submit using GH) and how to use React

Get React Developer Tools (Brave doesn't seem to be supported)

client (frontend, react) => server (backend, node, express, mongoose) => database (mongoDB)

Delete node_modules from your submission for work with CD. You should do this through your file explorer (not VS Code)


Babel is what makes the jsx (the html-looking stuff) convert into actual HTML.







